{
  "name": "awesome-dashboard-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Static dashboard sandbox with automated smoke tests",
  "scripts": {
    "dev": "python -m http.server 4173 --directory demo",
    "test": "node --test",
    "test:ci": "node --test"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "type": "module"
}
